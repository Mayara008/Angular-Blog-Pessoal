import { Postagem } from "./Postagem";

export class tema{
    public id: number;
    public descricao: string;
    public postagem: Postagem[] /* Vamos ter um array de postagem dentro de tema, é preciso colocar o simbolo de array no final de postagens (Conchetes ' [] ' ) a mesma coisa em User, tenho um array de resposta para cada user. ) */
    

}

